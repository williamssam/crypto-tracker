import * as React from 'react';
import {View, Text, Pressable, StyleSheet} from 'react-native';

const Home = () => {
  return (
    <View>
      <Text>Helloo, Welcome home</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Home;
